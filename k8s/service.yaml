apiVersion: v1
kind: Service
metadata:
  name: organization-service
  labels:
    app: organization-service
spec:
  type: NodePort
  selector:
    app: organization-service
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
